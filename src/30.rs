fn main() {
    // Define variables
    let x = 5;
    let y = 10;

    // Add the two numbers and store in z
    let z = x + y;

    // Print the result
    println!("The sum of {} and {} is: {}", x, y, z);
}
